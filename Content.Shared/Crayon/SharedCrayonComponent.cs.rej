diff a/Content.Shared/Crayon/SharedCrayonComponent.cs b/Content.Shared/Crayon/SharedCrayonComponent.cs	(rejected hunks)
@@ -80,13 +80,15 @@ public sealed class CrayonComponentState : ComponentState
         public readonly string State;
         public readonly int Charges;
         public readonly int Capacity;
+        public readonly bool Infinite; // imp
 
-        public CrayonComponentState(Color color, string state, int charges, int capacity)
+        public CrayonComponentState(Color color, string state, int charges, int capacity, bool infinite)
         {
             Color = color;
             State = state;
             Charges = charges;
             Capacity = capacity;
+            Infinite = infinite; // imp
         }
     }
 
