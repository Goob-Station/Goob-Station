light_mode unshaded;
blend_mode add;

void fragment() {
    highp float d = distance(UV, vec2(0.5, 0.5));
    highp float a = clamp(1.0 - d * 2.0, 0.0, 1.0);
    COLOR = vec4(1.0, 1.0, 1.0, a * a);
}
