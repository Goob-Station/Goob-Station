using Content.Client._pofitlo.CombatExtended.UserInterface.FightAction.UI;
using Content.Client._Shitmed.UserInterface.Systems.Targeting;
using Content.Client.UserInterface.Controls;
using Content.Shared._Shitmed.Targeting;
using Robust.Client.AutoGenerated;
using Robust.Client.Graphics;
using Robust.Client.UserInterface.Controls;
using Robust.Client.UserInterface.XAML;
namespace Content.Client._pofitlo.CombatExtended.UserInterface.FightAction.Widgets;

[GenerateTypedNameReferences]
public sealed partial class FightActionControl : UIWidget
{
    private SimpleRadialMenu? _menu;
    private FightActionRadialMenu? _fightActionMenu;
    private readonly TargetingUIController _controller;
    public FightActionControl()
    {
        RobustXamlLoader.Load(this);
        _controller = UserInterfaceManager.GetUIController<TargetingUIController>();

        TargetDoll.Texture = Theme.ResolveTexture("FightActions/FASlotBackground");
        test.OnPressed += _ => OnPressed();
    }

    private void SetActiveBodyPart(TargetBodyPart bodyPart) => _controller.CycleTarget(bodyPart);

    protected override void OnThemeUpdated() => TargetDoll.Texture = Theme.ResolveTexture("FightActions/FASlotBackground");

    public void SetTargetDollVisible(bool visible) => Visible = visible;

    private void OnPressed()
    {
        //if (args.Function != EngineKeyFunctions.UIClick && args.Function != EngineKeyFunctions.UIRightClick)
        //    return;

        var ui = UserInterfaceManager.GetUIController<FightActionUIController>();
        ui.ToggleMenu(test1.GlobalRect.BottomRight);
    }

    public void ChangeWidgetIcon(Texture texture)
    {
        ButtonTexture.Texture = texture;
    }
}
