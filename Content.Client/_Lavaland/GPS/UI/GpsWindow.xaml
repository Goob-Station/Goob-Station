<controls:FancyWindow
        xmlns="https://spacestation14.io"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
        xmlns:s="clr-namespace:Content.Client.Stylesheets"
        MinHeight="710"
        MinWidth="510">
    <BoxContainer Name="MainContainer" Orientation="Vertical">
        <!-- Current Location -->
        <BoxContainer Orientation="Horizontal" Margin="10 2 5 0" VerticalAlignment="Top">
            <Label Text="{Loc 'signal-locator-current-location'}" FontColorOverride="{x:Static s:StyleNano.NanoGold}" StyleClasses="LabelBig"/>
            <Label Name="LocalPosition" Text="{Loc 'signal-locator-location-unknown'}"/>
        </BoxContainer>
        <!-- Fancy buttons -->
        <BoxContainer Orientation="Vertical" Align="Center">
            <controls:StripeBack HorizontalExpand="True">
                <Button Name="RefreshButton" Text="{Loc 'signal-locator-refresh-button'}" Disabled="False" SetSize="96 32" Margin="5" HorizontalAlignment="Left"/>
                <OptionButton Name="ScanMode" Disabled="False" SetSize="192 32" Margin="5" HorizontalAlignment="Center"/>
                <OptionButton Name="RangeMode" Disabled="False" SetSize="192 32" Margin="5" HorizontalAlignment="Right"/>
            </controls:StripeBack>
        </BoxContainer>
        <!-- Search bar -->
        <BoxContainer Orientation="Horizontal">
    	    <LineEdit Name="SearchBar" PlaceHolder="{Loc 'signal-locator-search-filter'}" HorizontalExpand="True" Margin="4 4"/>
        </BoxContainer>
        <controls:SearchListContainer Name="DisplayedSignals" VerticalExpand="True" Margin="4 4"/>
         <!-- Footer -->
        <BoxContainer Orientation="Vertical">
            <PanelContainer StyleClasses="LowDivider"/>
            <BoxContainer Orientation="Horizontal" Margin="10 2 5 0" VerticalAlignment="Bottom">
                <Label Text="{Loc 'signal-locator-flavor-left'}" StyleClasses="WindowFooterText" />
                <Label Text="{Loc 'signal-locator-flavor-right'}" StyleClasses="WindowFooterText"
                     HorizontalAlignment="Right" HorizontalExpand="True"  Margin="0 0 5 0" />
                <TextureRect StyleClasses="NTLogoDark" Stretch="KeepAspectCentered"
                     VerticalAlignment="Center" HorizontalAlignment="Right" SetSize="19 19"/>
            </BoxContainer>
        </BoxContainer>
    </BoxContainer>
</controls:FancyWindow>
