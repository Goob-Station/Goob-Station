<DefaultWindow xmlns="https://spacestation14.io"
               Title="{Loc 'silicon-name-ui-menu'}"
               MinWidth="250">
    <BoxContainer HorizontalExpand="True">
        <Label Text="{Loc 'humanoid-profile-editor-borgname-label'}" />
        <Control HorizontalExpand="True"/>
        <LineEdit Name="BorgNameEdit" MinSize="270 0" HorizontalAlignment="Right" Margin="2, 2"/>
        <Button Name="BorgNameRandomize" Text="{Loc 'humanoid-profile-editor-name-random-button'}" />
    </BoxContainer>
</DefaultWindow>
