<DefaultWindow xmlns="https://spacestation14.io"
               xmlns:viewport="clr-namespace:Content.Client.Viewport"
               MinWidth="400" MinHeight="300">
  <!-- ScalingViewport eye hookup is done in code-behind -->
  <viewport:ScalingViewport Name="Viewport"
                            VerticalExpand="True"
                            HorizontalExpand="True"
                            MinSize="300 300"
                            MouseFilter="Ignore" />
</DefaultWindow>
