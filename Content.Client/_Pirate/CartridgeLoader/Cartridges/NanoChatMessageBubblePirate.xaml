<cartridges:NanoChatMessageBubblePirate
    xmlns="https://spacestation14.io"
    xmlns:cartridges="clr-namespace:Content.Client._Pirate.CartridgeLoader.Cartridges"
    xmlns:graphics="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
    HorizontalExpand="True">

    <BoxContainer Name="MessageContainer"
                  Orientation="Horizontal"
                  HorizontalExpand="True">
        <!-- Left spacer for other's messages -->
        <Control Name="LeftSpacer"
                 MinSize="0 0" />

        <!-- Message panel -->
        <BoxContainer Name="MessageBox"
                      Orientation="Vertical"
                      MaxWidth="278"
                      Margin="0 2"
                      HorizontalExpand="True">
            <PanelContainer Name="MessagePanel"
                            MaxWidth="278"
                            HorizontalExpand="True">
                <PanelContainer.PanelOverride>
                    <graphics:StyleBoxFlat
                        ContentMarginLeftOverride="10"
                        ContentMarginRightOverride="10"
                        ContentMarginTopOverride="6"
                        ContentMarginBottomOverride="6"
                        BorderThickness="0">
                        <!-- Colors set in code based on message sender -->
                    </graphics:StyleBoxFlat>
                </PanelContainer.PanelOverride>
                <BoxContainer Orientation="Vertical"
                              HorizontalExpand="True">
                    <Label Name="PhotoNameLabel"
                           StyleClasses="LabelSubText"
                           Visible="False"
                           ClipText="True"
                           Margin="0 0 0 4" />
                    <TextureRect Name="PhotoPreview"
                                 HorizontalExpand="True"
                                 MinSize="258 96"
                                 MaxSize="258 258"
                                 Stretch="Scale"
                                 Visible="False"
                                 Margin="0 0 0 6" />
                    <RichTextLabel Name="MessageText"
                                   HorizontalExpand="True"
                                   Visible="False" />
                    <Button Name="StorePhotoButton"
                            Text="{Loc nano-chat-store-photo}"
                            Visible="False"
                            HorizontalExpand="True"
                            HorizontalAlignment="Stretch"
                            MinSize="0 28"
                            Margin="0 6 0 0"
                            StyleClasses="OpenBoth" />
                </BoxContainer>
            </PanelContainer>

            <!-- Delivery failed text -->
            <Label Name="DeliveryFailedLabel"
                   Text="{Loc nano-chat-delivery-failed}"
                   StyleClasses="font-small"
                   HorizontalExpand="True"
                   HorizontalAlignment="Right"
                   Margin="8 2 8 0"
                   Visible="False" />
        </BoxContainer>

        <!-- Right spacer for own messages -->
        <Control Name="RightSpacer"
                 MinSize="6 0" />

        <!-- Flexible space for alignment -->
        <Control Name="FlexSpace"
                 HorizontalExpand="True" />
    </BoxContainer>
</cartridges:NanoChatMessageBubblePirate>



